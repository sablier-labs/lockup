Getters_Concrete_Test::getAirdropsMinFeeUSD
├── given min fee not set
│  └── it should zero
└── given min fee set
   └── it should return the correct min fee USD

Getters_Concrete_Test::getAirdropsMinFeeUSDFor
├── given custom fee USD not set
│  └── it should return min fee USD for campaign creator
└── given custom fee USD set
   └── it should return custom fee USD for campaign creator

Getters_Concrete_Test::getFlowMinFeeUSD
├── given min fee not set
│  └── it should zero
└── given min fee set
   └── it should return the correct min fee USD

Getters_Concrete_Test::getFlowMinFeeUSDFor
├── given custom fee USD not set
│  └── it should return min fee USD for sender
└── given custom fee USD set
   └── it should return custom fee USD for sender

Getters_Concrete_Test::getLockupMinFeeUSD
├── given min fee not set
│  └── it should zero
└── given min fee set
   └── it should return the correct min fee USD

Getters_Concrete_Test::getLockupMinFeeUSDFor
├── given custom fee USD not set
│  └── it should return min fee USD for campaign creator
└── given custom fee USD set
   └── it should return custom fee USD for campaign creator

Getters_Concrete_Test::oracle
├── given oracle not set
│  └── it should return zero address
└── given oracle set
   └── it should return the correct oracle address
