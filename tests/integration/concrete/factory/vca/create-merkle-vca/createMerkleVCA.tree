CreateMerkleVCA_Integration_Test
├── when native token found
│  └── it should revert
└── when native token not found
   ├── given campaign already exists
   │  └── it should revert
   └── given campaign not exists
      ├── when start time zero
      │  └── it should revert
      └── when start time not zero
         ├── when end time less than start time
         │  └── it should revert
         ├── when end time equals start time
         │  └── it should revert
         └── when end time greater than start time
            ├── when zero expiry
            │  └── it should revert
            └── when not zero expiry
               ├── when expiry not exceed one week from end time
               │  └── it should revert
               └── when expiry exceeds one week from end time
                  ├── given custom fee set
                  │  ├── it should create the campaign with custom fee
                  │  ├── it should set the current factory address
                  │  ├── it should set return the correct unlock timestamps
                  │  └── it should emit a {CreateMerkleVCA} event
                  └── given custom fee not set
                     ├── it should create the campaign with minimum fee
                     ├── it should set the current factory address
                     ├── it should set return the correct unlock timestamps
                     └── it should emit a {CreateMerkleVCA} event
