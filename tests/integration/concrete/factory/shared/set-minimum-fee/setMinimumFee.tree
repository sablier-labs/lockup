SetMinimumFee_Integration_Test
├── when caller not admin
│   └── it should revert
└── when caller admin
    ├── when new minimum fee exceeds the maximum fee
    │   └── it should revert
    └── when new minimum fee does not exceed the maximum fee
        ├── it should set the minimum fee
        └── it should emit a {setMinimumFee} event