OnlyRole_RoleAdminable_Concrete_Test
├── when caller admin
│  └── it should execute the function
└── when caller not admin
   ├── when caller not have role
   │  └── it should revert
   └── when caller has role
      └── it should execute the function
