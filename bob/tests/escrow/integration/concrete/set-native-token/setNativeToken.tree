SetNativeToken_Integration_Concrete_Test
├── when caller not comptroller
│  └── it should revert
└── when caller comptroller
   ├── when provided address zero
   │  └── it should revert
   └── when provided address not zero
      ├── given native token already set
      │  └── it should revert
      └── given native token not set
         ├── it should set the native token
         └── it should emit a {SetNativeToken} event
