SetTradeFee_Integration_Concrete_Test
├── when caller not comptroller
│  └── it should revert
└── when caller comptroller
   ├── when new fee exceeds max
   │  └── it should revert
   └── when fee not exceed max
      ├── it should set the new trade fee
      └── it should emit a {SetTradeFee} event
