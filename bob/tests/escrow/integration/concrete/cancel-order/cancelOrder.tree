CancelOrder_Integration_Concrete_Test
├── given null
│  └── it should revert
└── given not null
   ├── when caller not seller
   │  └── it should revert
   └── when caller seller
      ├── given filled
      │  └── it should revert
      ├── given canceled
      │  └── it should revert
      ├── given expired
      │  ├── it should cancel the order
      │  └── it should emit a {CancelOrder} event
      └── given open
         ├── it should cancel the order
         └── it should emit a {CancelOrder} event
