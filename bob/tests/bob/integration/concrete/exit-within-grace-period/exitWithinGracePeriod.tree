ExitWithinGracePeriod_Integration_Concrete_Test
├── given null
│  └── it should revert
└── given not null
   ├── given settled
   │  └── it should revert
   ├── given expired
   │  └── it should revert
   └── given active
      ├── when no shares to redeem
      │  └── it should revert
      └── when caller has shares
         ├── when caller not original depositor
         │  └── it should revert
         └── when caller is original depositor
            ├── when grace period expired
            │  └── it should revert
            └── when within grace period
               ├── given no adapter
               │  └── it should exit and return tokens
               └── given adapter
                  └── it should exit via adapter unstake
