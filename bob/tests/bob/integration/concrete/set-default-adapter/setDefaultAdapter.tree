SetDefaultAdapter_Integration_Concrete_Test
├── when caller not comptroller
│  └── it should revert
└── when caller comptroller
   ├── when adapter not zero address
   │  ├── when adapter does not support interface
   │  │  └── it should revert
   │  └── when adapter supports interface
   │     └── it should set adapter for token
   └── when adapter zero address
      └── it should disable adapter for token
