Constructor_LidoAdapter_Integration_Concrete_Test
├── when slippage tolerance exceeds maximum
│   └── it should revert
├── when yield fee exceeds maximum
│   └── it should revert
└── when parameters are valid
    └── it should set all state correctly
