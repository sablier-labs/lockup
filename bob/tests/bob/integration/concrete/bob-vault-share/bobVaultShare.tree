BobVaultShare::decimals
└── it should return the decimals set in constructor

BobVaultShare::mint
├── when caller is not SablierBob
│  └── it should revert
└── when caller is SablierBob
   └── it should mint tokens

BobVaultShare::burn
├── when caller is not SablierBob
│  └── it should revert
└── when caller is SablierBob
   └── it should burn tokens
