ClaimAndExecute_MerkleExecute_Integration_Test
├── given campaign start time in future
│  └── it should revert
└── given campaign start time not in future
   ├── given campaign expired
   │  └── it should revert
   └── given campaign not expired
      ├── given msg value less than fee
      │  └── it should revert
      └── given msg value not less than fee
         ├── given index claimed
         │  └── it should revert
         └── given index not claimed
            ├── when index not valid
            │  └── it should revert
            └── when index valid
               ├── when amount not valid
               │  └── it should revert
               └── when amount valid
                  ├── when merkle proof not valid
                  │  └── it should revert
                  └── when merkle proof valid
                     ├── when reentrancy
                     │  └── it should revert
                     ├── when target call fails
                     │  └── it should revert with target error
                     └── when target call succeeds
                        ├── it should mark the index as claimed
                        ├── it should transfer the ETH to the comptroller
                        ├── it should execute the target call
                        └── it should emit a {ClaimExecute} event
