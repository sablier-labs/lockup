CreateWithDurationsLPG_Integration_Concrete_Test
├── when delegate call
│  └── it should revert
└── when no delegate call
   ├── when oracle address zero
   │  └── it should revert
   └── when oracle address not zero
      ├── when oracle misses decimals
      │  └── it should revert
      └── when oracle not miss decimals
         ├── when oracle decimals not 8
         │  └── it should revert
         └── when oracle decimals 8
            ├── when oracle misses latestRoundData
            │  └── it should revert
            └── when oracle not miss latestRoundData
               ├── when oracle price not positive
               │  └── it should revert
               └── when oracle price positive
                  ├── when target price not exceed current price
                  │  └── it should revert
                  └── when target price exceeds current price
                     ├── when duration zero
                     │  └── it should revert
                     └── when duration not zero
                        ├── it should create the stream
                        ├── it should bump the next stream ID
                        ├── it should store the unlock params
                        ├── it should mint the NFT
                        ├── it should perform the ERC-20 transfer
                        └── it should emit {CreateLockupPriceGatedStream} and {MetadataUpdate} events
