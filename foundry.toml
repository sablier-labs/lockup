[profile.default]
  ast = true                       # needed for upgrade safety validations by foundry-upgrades
  auto_detect_solc = false
  build_info = true                # needed for upgrade safety validations by foundry-upgrades
  bytecode_hash = "ipfs"
  evm_version = "shanghai"         # needed for greater coverage of EVM chains
  extra_output = ["storageLayout"] # needed for upgrade safety validations by foundry-upgrades
  fs_permissions = [
    { access = "read", path = "./out-optimized" },
    { access = "read", path = "package.json" },
  ]
  ffi = true                        # needed for upgrade safety validations by foundry-upgrades
  gas_reports = ["*"]
  optimizer = true
  optimizer_runs = 10_000
  out = "out"
  script = "scripts"
  solc = "0.8.29"
  src = "src"
  test = "tests"

[profile.default.fuzz]
  max_test_rejects = 1_000_000 # Number of times `vm.assume` can fail
  runs = 1000

[profile.default.invariant]
  call_override = false  # Override unsafe external calls to perform reentrancy checks
  depth = 100            # Number of calls executed in one run
  fail_on_revert = true
  runs = 200
  show_metrics = false

# Compile only the production code and the test mocks with via IR
[profile.optimized]
  optimizer = true
  optimizer_runs = 100_000_000
  out = "out-optimized"
  test = "src"
  via_ir = true

[fmt]
  bracket_spacing = true
  int_types = "long"
  line_length = 120
  multiline_func_header = "all"
  number_underscore = "thousands"
  quote_style = "double"
  tab_width = 4
  wrap_comments = true

[lint]
  lint_on_build = false

[rpc_endpoints]
  # mainnets
  arbitrum = "https://lb.routeme.sh/rpc/42161/${ROUTEME_API_KEY}"
  avalanche = "https://lb.routeme.sh/rpc/43114/${ROUTEME_API_KEY}"
  base = "https://lb.routeme.sh/rpc/8453/${ROUTEME_API_KEY}"
  berachain = "https://rpc.berachain.com"
  blast = "https://rpc.blast.io"
  bsc = "https://lb.routeme.sh/rpc/56/${ROUTEME_API_KEY}"
  ethereum = "${MAINNET_RPC_URL}"
  chiliz = "https://rpc.chiliz.com"
  core_dao = "https://rpc.coredao.org"
  form = "https://rpc.form.network/http"
  gnosis = "https://lb.routeme.sh/rpc/100/${ROUTEME_API_KEY}"
  lightlink = "https://replicator.phoenix.lightlink.io/rpc/v1"
  linea = "https://lb.routeme.sh/rpc/59144/${ROUTEME_API_KEY}"
  meld = "https://rpc-1.meld.com"
  mode = "https://mainnet.mode.network/"
  morph = "https://rpc.morphl2.io"
  optimism = "https://lb.routeme.sh/rpc/10/${ROUTEME_API_KEY}"
  polygon = "https://lb.routeme.sh/rpc/137/${ROUTEME_API_KEY}"
  scroll = "https://lb.routeme.sh/rpc/534352/${ROUTEME_API_KEY}"
  sei = "https://evm-rpc.sei-apis.com"
  superseed = "https://mainnet.superseed.xyz"
  xdc = "https://rpc.xdc.org"
  # testnets
  arbitrum_sepolia = "https://arbitrum-sepolia-rpc.publicnode.com"
  base_sepolia = "https://sepolia.base.org"
  berachain_artio = "https://bartio.rpc.berachain.com/"
  blast_sepolia = "https://sepolia.blast.io"
  linea_sepolia = "https://rpc.sepolia.linea.build"
  localhost = "http://localhost:8545"
  mode_sepolia = "https://sepolia.mode.network/"
  monad_testnet = "https://testnet-rpc.monad.xyz"
  morph_holesky = "https://rpc-holesky.morphl2.io"
  optimism_sepolia = "https://sepolia.optimism.io"
  sei_testnet = "https://evm-rpc.arctic-1.seinetwork.io"
  sepolia = "${SEPOLIA_RPC_URL}"
  superseed_sepolia = "https://sepolia.superseed.xyz"
