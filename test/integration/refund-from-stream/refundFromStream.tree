refundFromStream.t.sol
├── when delegate called
│  └── it should revert
└── when not delegate called
   ├── given the id references a null stream
   │  └── it should revert
   └── given the id does not reference a null stream
      ├── when the caller is not the sender
      │  ├── when the caller is the recipient
      │  │  └── it should revert
      │  └── when the caller is a malicious third party
      │     └── it should revert
      └── when the caller is the sender
         ├── when the refund amount is zero
         │  └── it should revert
         └── when the refund amount is not zero
            ├── when the refund amount overrefund
            │  └── it should revert
            └── when the refund amount does not overrefund
               ├── given the id references a paused stream
               │  └── it should refund from stream
               └── given the id does not reference a paused stream
                  ├── given the asset does not have 18 decimals
                  │  ├── it should make the refund
                  │  ├── it should update the stream balance
                  │  ├── it should perform the ERC-20 transfer
                  │  └── it should emit a {Transfer} and {RefundFromFlowStream} event
                  └── given the asset has 18 decimals
                     ├── it should make the refund   
                     ├── it should update the stream balance
                     ├── it should perform the ERC-20 transfer
                     └── it should emit a {Transfer} and {RefundFromFlowStream} event